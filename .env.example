# Server-side secrets — set in Vercel dashboard and local .env.local (never VITE_-prefixed)
MONGODB_URI=mongodb+srv://user:password@swipewise-db.uhhbmxq.mongodb.net/swipewise_admin
MONGODB_DB_NAME=swipewise_admin
ADMIN_EMAIL=admin@swipewise.com
ADMIN_PASSWORD=change-me
GEMINI_API_KEY=your_gemini_api_key
JWT_SECRET=change-me-to-a-long-random-string

# Optional client convenience — pre-fills login email only (password stays server-side)
VITE_ADMIN_EMAIL=admin@swipewise.com
